apiVersion: v1
kind: PersistentVolume
metadata:
  name: redis-pv
  namespace: sandee-staging
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: "/mnt/data/redis"
